package com.swimweek.app

import org.junit.Assert.assertTrue
import org.junit.Test

/** Placeholder unit test so the test source set is wired. */
class SmokeTest {
    @Test
    fun scaffoldIsWired() {
        assertTrue(true)
    }
}
