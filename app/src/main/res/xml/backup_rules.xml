<?xml version="1.0" encoding="utf-8"?>
<!-- Health-derived DataStores must never leave the device. allowBackup=false in manifest. -->
<full-backup-content>
    <exclude domain="sharedpref" path="." />
    <exclude domain="file" path="datastore/" />
</full-backup-content>
