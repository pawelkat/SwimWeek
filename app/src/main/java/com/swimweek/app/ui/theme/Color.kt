package com.swimweek.app.ui.theme

import androidx.compose.ui.graphics.Color

/** True black — AMOLED-safe background (not Material dark gray). */
val AmoledBlack = Color(0xFF000000)

/** Dim monochrome text for secondary labels on pure black. */
val AmoledTextPrimary = Color(0xFFE0E0E0)
val AmoledTextSecondary = Color(0xFF9E9E9E)
val AmoledTextMuted = Color(0xFF616161)

/** Subtle accent (low-saturation teal) for primary actions only. */
val AmoledAccent = Color(0xFF4DB6AC)
val AmoledError = Color(0xFFEF9A9A)
